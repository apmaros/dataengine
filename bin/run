#!/usr/bin/env sh

APP_NAME=dataengine

export PYTHONPATH="${PYTHONPATH}:./${APP_NAME}"
echo "Starting application server for ${APP_NAME}"
gunicorn -w 4 -b "${SERVER_HOST}":"${SERVER_PORT}" "${APP_NAME}":app
